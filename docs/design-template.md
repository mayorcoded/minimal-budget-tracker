# Design — [Project Name]

Superseded by Claude Design. Don't fill this in by hand — build the
prototype at claude.ai/design instead.

When the prototype is ready, hand it off from Claude Design's side:

> Export → Handoff to Claude Code → Send to local coding agent

That drops a bundle (HTML/CSS/JS + screenshots + README) that Claude
Code opens directly in this repo — no manual rebuilding, no MCP sync.

This file is kept only as a placeholder/pointer — delete it once the
Claude Design project exists.
